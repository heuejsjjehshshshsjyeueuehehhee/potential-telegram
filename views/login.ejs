<!DOCTYPE html>
<html>
<head>
    <title>Login</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="auth-container">
        <div class="auth-box">
            
            <% if(typeof site !== 'undefined' && site.logoNormal) { %>
                <img src="<%= site.logoNormal %>" style="width: 80px; margin-bottom: 20px;">
            <% } else { %>
                <h2 style="color:var(--primary); margin-bottom:10px;">WeebDuniya</h2>
            <% } %>

            <p style="color:#aaa; font-size:12px; margin-bottom:30px;">Admin Login</p>
            
            <% if(error) { %>
                <div style="background:rgba(231, 76, 60, 0.2); color:#e74c3c; padding:10px; border-radius:8px; margin-bottom:15px; font-size:13px;">
                    <%= error %>
                </div>
            <% } %>

            <form action="/auth/login" method="POST">
                <input type="text" name="username" class="auth-input" placeholder="Username" required>
                <input type="password" name="password" class="auth-input" placeholder="Password" required>
                <button type="submit" class="auth-btn">LOGIN</button>
            </form>

            <div style="margin-top:20px;">
                <a href="/" style="color:#888; font-size:12px;">← Back to Home</a>
            </div>
        </div>
    </div>
</body>
</html>